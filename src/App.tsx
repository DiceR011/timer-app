import React from "react";
import { Timer } from "./components";

const App: React.FC = () => {
    return (
        <div className="bg-gray-100">
            <Timer />
        </div>
    );
};

export default App;
